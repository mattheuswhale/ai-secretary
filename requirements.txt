# Voice Assistant Python dependencies
# Install with: pip install -r requirements.txt

# Speech-to-Text
faster-whisper>=1.0.0

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24.0

# TTS -> Kokoro (hexgrad/kokoro)
# Handles EN/KO natively; JA needs misaki[ja] extra + system espeak-ng
#
#   pip install kokoro>=0.9.4
#   pip install misaki[ja]                    # Japanese G2P support
#   sudo apt install espeak-ng                # Linux
#   brew install espeak-ng                    # macOS
#   # Windows: https://github.com/espeak-ng/espeak-ng/releases
#   fugashi needs unidic: python -m unidic download
kokoro>=0.9.4
pyopenjtalk
fugashi[unidic]
jaconv
mojimoji
# Web search (no API key required)
#duckduckgo-search>=6.0.0 replaced by ddgs
ddgs
# HTTP (weather agent + Ollama)
# NOTE: CVE-2024-47081 (.netrc credential leak) fixed in 2.32.4 //// do not lower this pin
requests>=2.32.4
